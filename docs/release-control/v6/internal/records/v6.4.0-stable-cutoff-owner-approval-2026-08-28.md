# v6.4.0 Stable Cutoff Owner Approval

- Decision date: `2026-08-28`
- Release: `v6.4.0`
- Runtime content cutoff: `18b22d1ebbfe542484652e419320fc7643a792f0`
- Promoted published prerelease: `v6.4.0-rc.12`
- Rollback target: `v6.3.2`
- Exact rollback reinstall command: `./scripts/install.sh --version v6.3.2`

## Owner Decision

The release owner explicitly directed stable `v6.4.0` publication from the
latest `main` cutoff and authorized shortening the normal 72-hour prerelease
soak. The approval covers the bounded product fixes after published
`v6.4.0-rc.12` and the final release-preparation metadata commit. The in-repo
`v6.4.0-rc.13` packet was not published and is not represented as additional
public prerelease lineage.

The accepted reason is active customer harm from incorrect same-name Proxmox
agent links, repeated inspection load on stopped-container-heavy Docker hosts,
incomplete Proxmox backup and TrueNAS SMART detail, and unstable rolling metric
history. Promoting the bounded corrections avoids extending those known harms
only to manufacture another candidate observation window.

This is a one-version owner-risk acceptance, not 72-hour soak evidence and not
a standing exception for later releases. The workflow input
`hotfix_exception=true` carries the approved waiver through the shared
promotion resolver. It does not reclassify this stable minor release as a patch
hotfix.

The final release-preparation commit may change only governed version,
release-note, qualification, test-guardrail, and release-control metadata. The
normal single-build release workflow must consume the pushed exact SHA and pass
its integrated frontend, backend, mobile, candidate, signing-policy, staging,
activation, and convergence gates. Current policy does not require a separate
Release Dry Run because the publication workflow runs the same exact-SHA
preflight before crossing the release boundary.

## Rollback And Compatibility Boundary

The rollback target remains stable `v6.3.2` using the exact command above.
Pulse Mobile iOS build 12 and Android versionCode 9 already support
`action_type=view_alert`. The v6.4.0 server and Unified Agent changes do not
alter mobile routes, pairing, push, or resource payload contracts, so the
decision is `existing-mobile-build-compatible` and no companion upload is
required.

The standing Windows Authenticode-unavailable policy from v6.3.2 applies
independently. Windows agents remain bound to the exact release SHA, immutable
candidate manifest, SHA-256 checksums, detached `.sig` and `.sshsig`
signatures, and published-digest verification. Public notes must disclose that
the binaries are not Authenticode-signed and may display an Unknown Publisher
warning.

## Publication Outcome

Canonical release run `33223712880` qualified exact source SHA
`58b07ef36cc815c2309ba43874ca5365f928ff77` without rebuilding on failed-job
attempt 2. Activation-only recovery run `33227705135` then published GitHub
release `378812881` at `2026-08-29T01:57:20Z`; annotated tag `v6.4.0`
dereferences that exact qualified source SHA, the release is neither draft nor
prerelease, and GitHub reports it as the latest stable release. The published
packet contains 223 assets, including the immutable candidate payload, the four
body-referenced release-note visuals, `release-activation.json` with server
digest `sha256:1489e1e2649d5a589b5776706954308028b88f3e138d29332ac064e1b28138ba`,
and the convergence owner receipt. Two stale unreferenced draft screenshots
were removed before activation; the exact-asset verifier rejects any such
unreferenced visual on future recovery.

Convergence run `33227719830` successfully moved Docker stable aliases, the
private paid-runtime broker, and public Helm Pages to the activated candidate.
The stable demo process did not bind health while restoring its inherited mock
runtime configuration, so the convergence owner was cancelled rather than
left holding the global promotion lease; lease release succeeded. Dedicated
current-bits-only recovery retained the exact v6.4.0 binary, systemd unit,
drop-ins, billing state, and Relay process while isolating the startup block to
an inherited local-development metrics-store backfill opt-in and unbounded
compatibility history density.

Governed recovery run `33230391189` on main SHA
`0443bcf17467297aeb1afc319fac073560b21b09` closed the remaining demo surface.
Its retained receipt records `status=recovered`, exact installed version
`6.4.0`, unchanged binary, unit, drop-in, and runtime-configuration digests,
and an unchanged Relay PID (`224055`). The recovered bounded public profile is
eight mock nodes with a two-hour seed, five-minute samples, and 15-second live
updates. Public health then reported the monitor, scheduler, and WebSocket
dependencies healthy; `/api/version` reported stable `6.4.0`, latest version
`6.4.0`, and no update available; frontend parity passed; and the authenticated
browser workspace reached the visible backend-and-stream connected status in
1.357 seconds. Compensation was correctly skipped after success. The original
convergence demo leg remains honestly recorded as cancelled; this separate
current-bits-only recovery supplies its bounded operational closure without
rebuilding or changing any v6.4.0 release artifact.

Convergence attempt 4 subsequently rechecked the already-published surfaces.
An invalid coordination ref that pointed at an ordinary main commit rather
than a typed lease owner was removed with an exact-SHA guard; the attempt then
reconfirmed Helm Pages, Docker aliases, and the paid-runtime broker. Its
already-current demo path nevertheless restarted the service while restoring
configuration and was cancelled at the bounded window before browser proof.
The convergence verdict failed honestly and its release-lease job succeeded.
Final recovery run `33230847710` on main SHA
`58e58553d79b1248d825d4944d111c4a3c2d0b58` restored the governed bounded
profile without replacing the installed binary, unit, or drop-ins, preserved
Relay PID `224055`, and passed public health, frontend parity, and authenticated
browser proof; the workspace reached connected status in 1.465 seconds. This
is the terminal demo state for the v6.4.0 cut. No further convergence rerun is
required because every customer surface has now been separately re-proved and
the exact release packet was unchanged throughout recovery.
