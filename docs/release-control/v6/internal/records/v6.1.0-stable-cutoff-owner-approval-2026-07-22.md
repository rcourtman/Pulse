# v6.1.0 Stable Cutoff Owner Approval

- Decision date: `2026-07-22`
- Release: `v6.1.0`
- Content cutoff base: `1f2a086535e281131171de19bfc78e5990bbeaca`
- Rollback target: `v6.0.5`
- Exact rollback reinstall command: `./scripts/install.sh --version v6.0.5`

## Owner Decision

The release owner ended the moving v6.1.0 RC feedback loop and directed one
stable cutoff from current `main`. The prepared `v6.1.0-rc.5` tag and GitHub
release were never created. v6.1.0 may therefore publish stable without another
RC publication or the normal 72-hour soak.

This is a one-version owner-risk acceptance, not validation evidence and not a
standing policy for later releases. The final release-preparation commit may
change only governed version, release-note, qualification, and release-control
metadata on top of the content cutoff base. The exact publish SHA must be bound
by the no-public-release `Release Dry Run` and then consumed by the normal
single-build release workflow without further product changes.

## Windows Signing Exception

The first exact-SHA stable rehearsal, GitHub Actions run `29927692302`, stopped
before candidate assembly because the external SignPath configuration was
unavailable. It created no tag, draft, or public release. The release owner
then explicitly approved publishing `v6.1.0` without Windows Authenticode
signing rather than entering another release-candidate loop.

This is a `v6.1.0`-only exception. Windows Unified Agent binaries remain bound
to the exact source SHA and candidate manifest and must retain checksum,
detached `.sig`/`.sshsig`, and post-publication digest verification. The public
release notes must state that the Windows binaries are not
Authenticode-signed and may show an Unknown Publisher warning. Every later
stable version restores the Windows Authenticode requirement automatically.

## Cutoff Reopen Rule

Only a security defect, data-loss risk, broken upgrade, startup failure, or
release-pipeline defect may reopen v6.1.0. Ordinary product findings, polish,
and non-critical feedback after the cutoff belong in v6.1.1.

## Commercial Boundary

This approval does not authorize production billing mutation or activation of
unproved self-service Relay/Pro plan and cadence transitions. Those transitions
remain unavailable and unadvertised until lane follow-up
`self-hosted-commercial-transition-coherence` and its L3/L17 companions
`cloud-commercial-transition-external-proof` and
`account-commercial-transition-external-proof` complete the offer, isolated
Stripe lifecycle, production Relay/Pulse version-floor, and buyer-journey
proof. Release gate
`self-hosted-commercial-transition-exposure-safety` remains responsible for
proving that disabled exposure boundary.
