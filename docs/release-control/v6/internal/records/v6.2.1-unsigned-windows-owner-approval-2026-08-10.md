# v6.2.1 Unsigned Windows Owner Approval

- Decision date: `2026-08-10`
- Release: `v6.2.1`
- Failed release run: `31343128024`
- Failed release SHA: `5a865820a6e48ff479e487fe95ccaac9dfefda86`
- Rollback target: `v6.2.0`
- Exact rollback reinstall command: `./scripts/install.sh --version v6.2.0`

## Owner Decision

Release run `31343128024` failed closed before candidate assembly or public
mutation because SignPath rejected the signing request while the
`release-signing` policy was invalid. The authoritative SignPath project state
showed `Release certificate 2026` with `CSR PENDING`.

On 2026-08-10, the release owner explicitly authorized unsigned Windows
Unified Agent artifacts for `v6.2.1` so the external certificate delay does not
block the qualified emergency patch. This is a version-bound risk acceptance,
not evidence that Authenticode succeeded and not a standing exception for later
releases. Stable `v6.2.2` and later restore mandatory Authenticode unless a new
explicit, version-bound owner decision is recorded.

## Required Integrity Controls

The exception changes only the Authenticode requirement. Windows artifacts
must still be built from the exact release SHA and bound to the immutable
candidate manifest, SHA-256 checksums, detached `.sig` and `.sshsig`
signatures, and published-digest verification. The public release notes must
state that the Windows binaries are not Authenticode-signed and may display an
Unknown Publisher warning.

The normal integrated frontend, backend, installer, Docker, Helm, macOS
signing and notarization, candidate validation, publication, activation, and
convergence controls remain unchanged.
