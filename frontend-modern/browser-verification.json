{
  "version": 1,
  "base_sha": "c3213510e6719d486b46fd226b7a208cff375ebd",
  "verified_at": "2026-09-22T03:24:00Z",
  "result": "passed",
  "changed_paths": [
    "frontend-modern/src/features/proxmox/ProxmoxBackupServersTable.tsx"
  ],
  "content_sha256": {
    "frontend-modern/src/features/proxmox/ProxmoxBackupServersTable.tsx": "18535683c37f5b5a481ac012a49b7dcdd6264ac992076381c74fdfbb037e31eb"
  },
  "routes": [
    "/proxmox/backups (production surface represented by the component fixture)",
    "/browser-tests/pbs-host-history-correlation.html"
  ],
  "viewports": [
    {
      "width": 1280,
      "height": 900
    },
    {
      "width": 390,
      "height": 844
    }
  ],
  "states": [
    "The Backups PBS server row renders for a host whose agent is surfaced as both a PVE guest and a standalone source=pbs host row",
    "Expanding the row opens the canonical drawer; History is selectable and renders the host metric groups",
    "The drawer's merged header shows the guest's current CPU/memory (15.4% / 16.9%) rather than the PBS service values, confirming the guest presentation is merged",
    "The History request resolves to the guest series vm/proxmox:100; the PBS service target agent/pbs-1 is never requested",
    "The same correlation holds at the narrow 390px layout"
  ],
  "interactions": [
    "Mount the production ProxmoxBackupServersTable with synthetic PBS server, PVE guest and standalone agent resources sharing one agent identity",
    "Focus the row disclosure button and activate it with Enter, then select the History subtab",
    "Intercept /api/metrics-store/history requests and assert the requested resourceType/resourceId pair",
    "Repeat at 1280x900 and 390x844 and capture a screenshot of each rendered History tab",
    "Record Chromium from the offline Playwright runtime with no uncaught page errors"
  ],
  "command": "pulse-worker-browser scripts/check-pbs-host-history-correlation.cjs",
  "notes": "Offline Playwright Chromium run of the production ProxmoxBackupServersTable component, not a live Pulse. The fixture feeds synthetic resources only and the check script answers /api/metrics-store/history with empty points, so the panels correctly show the collecting-history state; the assertion is the requested history target, which in production carries the persisted host series. No backend, WebSocket or credential path is exercised. This receipt is content-addressed to the changed frontend source at this candidate base."
}
