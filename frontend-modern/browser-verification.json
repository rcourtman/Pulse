{
  "version": 1,
  "result": "passed",
  "base_sha": "14a82e7684390c9fe0a4bd900417c4adbb7140b8",
  "verified_at": "2026-08-07T22:34:00Z",
  "changed_paths": [
    "frontend-modern/src/api/runtimeInventorySources.ts",
    "frontend-modern/src/components/Workloads/WorkloadsSurface.tsx",
    "frontend-modern/src/components/Workloads/useWorkloadsState.ts",
    "frontend-modern/src/components/Workloads/workloadInventorySourceIssues.ts"
  ],
  "content_sha256": {
    "frontend-modern/src/api/runtimeInventorySources.ts": "9e232e6614ea8be4c4381b8211e70c630e5e55257f7dff0a4700b6874aad25f2",
    "frontend-modern/src/components/Workloads/WorkloadsSurface.tsx": "27b72d5663a71c1cc23b2fab2631a33b53bfea5e73371609e501afa682364f3c",
    "frontend-modern/src/components/Workloads/useWorkloadsState.ts": "15ac1786d045306add2c262e5f98f9560356274541c93ac604144336d2263f66",
    "frontend-modern/src/components/Workloads/workloadInventorySourceIssues.ts": "21500acb325413aed230f970c748ff9e9ed81405d12128473223b51507b677d9"
  },
  "routes": [
    "/infrastructure (Proxmox overview: nodes table + embedded workloads surface, which owns the inventory source issues banner)"
  ],
  "viewports": [
    {
      "width": 1280,
      "height": 800
    },
    {
      "width": 375,
      "height": 812
    }
  ],
  "states": [
    "authenticated NON-ADMIN viewer session (proxy auth, X-Remote-Role: viewer) against a scratch backend on 8920 behind a header-injecting proxy on 8922; /api/security/status reported detailLevel 'authenticated' and settingsCapabilities.infrastructureRead false, the same session tier that previously got nothing",
    "authenticated ADMIN control session (X-Remote-Role: admin, proxy on 8921) reporting detailLevel 'privileged' and infrastructureRead true, used as the A/B for the identical-projection claim",
    "one workload-capable source in a blocking state: a PVE connection configured at https://10.255.255.1:8006 (unroutable) so it settles into an unreachable state carrying vms + containers coverage",
    "banner rendered for the viewer at both viewports reading 'Source unreachable: scratch-pve / Pulse has VMs and containers enabled for scratch-pve, but the Proxmox VE API is unreachable. / VMs and containers' — before this change the same session saw no banner at all",
    "no raw-error detail line on the banner in either tier, and the rendered page text contained neither the source address '10.255.255.1' nor the administrative stateReason 'awaiting first poll'"
  ],
  "interactions": [
    "compared the two tiers at the API boundary: viewer GET /api/connections returned HTTP 403 (the defect) while viewer GET /api/runtime/inventory-sources returned HTTP 200 with the narrow envelope {id, type, name, state, surfaces, credentialsInvalid}; the admin GET /api/connections for the same source additionally carried address 'https://10.255.255.1:8006', hostAliases ['scratch-pve','10.255.255.1'] and stateReason, none of which appear in the projection",
    "logged every viewer API call through the proxy across ~50s (3+ background refetch cycles) at 1280x800: GET /api/runtime/inventory-sources -> 200 x4 (initial mount plus background refetches at the 15s poll interval) and zero GET /api/connections calls of any status",
    "counted admin-endpoint denials in the backend log over the same window: 0 lines matching 'non-admin' / 'attempted to access admin endpoint', down from one denial per mount plus one per 15s refetch",
    "reloaded /infrastructure at 375x812 as the viewer and confirmed the banner still renders with identical copy, with documentElement.scrollWidth not exceeding the 375px viewport (no horizontal overflow)",
    "loaded /infrastructure as the ADMIN session at 1280x800 and confirmed the banner renders byte-identical text to the viewer, proving the projection is not privilege-dependent and that admins keep the warning"
  ]
}
