{
  "version": 1,
  "result": "passed",
  "base_sha": "a8775495eaef979d3c454ff713c263e64020d1ed",
  "verified_at": "2026-08-07T22:02:36Z",
  "changed_paths": [
    "frontend-modern/src/components/Settings/settingsNavCatalog.ts",
    "frontend-modern/src/types/config.ts"
  ],
  "content_sha256": {
    "frontend-modern/src/components/Settings/settingsNavCatalog.ts": "95d92142e693a16641f462b7fd4f30c5ef05ed61a56dc8989c2f452c63c18004",
    "frontend-modern/src/types/config.ts": "9dc0faa8e9b41eab526245aae72e447535709108c0b109c76545ab73500a2588"
  },
  "routes": [
    "/settings/monitoring/availability (this branch's gate)",
    "/settings/support/logs (this branch's gate)",
    "/settings/system-general (the non-admin landing tab both branches rely on)"
  ],
  "viewports": [
    {
      "width": 1280,
      "height": 800
    },
    {
      "width": 375,
      "height": 812
    }
  ],
  "states": [
    "merge of origin/main (6a958761e, systemSettingsRead gating the System tabs) into a8775495e (availabilityRead / pulseIntelligenceRead / diagnosticsRead / systemLogsRead / reportingRead). The two commits gate disjoint tabs from the same canAccessAdminSurface(settings:read) derivation, and settingsNavCatalog.ts and types/config.ts now carry both sets - which is exactly why the merged artifact needed re-exercising rather than inheriting either side's receipt",
    "re-verified against a binary built from the merged tree, behind the same two-identity header-injecting proxy-auth shim: viewer reports detailLevel=authenticated with all 21 settings capabilities false; admin reports detailLevel=privileged with all 21 true",
    "non-admin settings nav is now down to Plans & Billing, General and Security Overview - this branch removes Availability checks, the three Pulse Intelligence tabs and the three Support tabs, and main removes Network, Pulse server updates and Recovery. Neither side's gates were lost in the merge",
    "non-admin deep link to /settings/monitoring/availability still redirects to /settings/system-general, with no [role=alert] and no request to /api/availability-targets, /api/settings/ai, /api/diagnostics, /api/logs/* or /api/admin/reports/*",
    "admin keeps the full 22-item nav across both gate sets and Availability checks renders its real panel, so neither commit's capability withholds a surface from an entitled session",
    "residual unchanged and out of scope: the bootstrap GET /api/system/settings still answers 403 to a non-admin. It is the last remaining settings-related refusal and is addressed by the /api/runtime/display work on claude/frosty-brahmagupta-34865c"
  ],
  "interactions": [
    "curled /api/security/status through both proxies against the merged binary and compared the full 21-field capability map for viewer and admin before touching the browser",
    "loaded /settings/monitoring/availability as the viewer at 1280x800, recorded the settled URL, the empty [role=alert] set, the surviving nav entries, and every /api/ resource entry matching the gated routes",
    "loaded /settings/support/logs as the viewer at 375x812 and confirmed the same redirect to /settings/system-general with no gated request and no horizontal overflow",
    "loaded /settings/monitoring/availability as the admin at 1280x800 and enumerated all 22 nav entries plus the rendered Availability checks heading"
  ]
}
