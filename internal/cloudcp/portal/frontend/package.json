{
  "name": "pulse-account-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node dev.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "jsdom": "^24.1.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
